import React from "react";
import "./popup.styles.css";

export const ErrorPopup = ({ opts }) => {
  const { missingFields } = opts;

  console.log(fields.missing);

  return <div className="err-container">errorPopup</div>;
};
